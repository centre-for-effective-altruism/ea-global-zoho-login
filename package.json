{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn --cwd packages/next-js dev",
    "build": "yarn --cwd packages/next-js build",
    "start": "yarn --cwd packages/next-js start"
  }
}
